const fs = require('fs'); const html = fs.readFileSync('C:/Users/casus/.gemini/antigravity-cli/brain/a3bd9377-b32b-4139-9229-3069cc0ab4cf/.system_generated/steps/117/content.md', 'utf8'); const classes = new Set(); const matches = html.matchAll(/class=\"([^\"]+)\"/g); for (const match of matches) { match[1].split(/\s+/).forEach(c => classes.add(c)); } console.log(Array.from(classes).slice(0, 50).join(', '));
